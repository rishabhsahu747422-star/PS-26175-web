# Assets Videos Directory

Drop demonstration screen captures and pipeline videos here.

Default expected file name:
- `demo-pipeline.mp4`

When placed here, the video player on `index.html` and `visualizations.html` will automatically play the file directly in the browser. Alternatively, specify a YouTube URL inside `js/project-data.js`.
